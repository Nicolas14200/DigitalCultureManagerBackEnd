import { UserRepository } from "../../domain/repositories/UserRepository"

describe("Unit - SignIn", () => {
    it("Should SignIn a user", async () => {
        let inMemoryUserRepo: UserRepository;
    })
})